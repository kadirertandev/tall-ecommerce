<nav class="main-container flex justify-end gap-4 text-xs mt-1">
    <a href="{{ route('aboutus') }}" class="text-gray-500">{{ __('frontend.aboutus') }}</a>
    <a href="{{ route('help-support') }}" class="text-gray-500">{{ __('frontend.help-support') }}</a>
</nav>
